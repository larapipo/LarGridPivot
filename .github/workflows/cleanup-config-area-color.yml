name: Cleanup config area color patch
on:
  push:
    branches: [feature-config-area-color-final]
permissions:
  contents: write
jobs:
  cleanup:
    if: "!contains(github.event.head_commit.message, '[config-cleanup-applied]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature-config-area-color-final
      - run: python cleanup_config_area_color.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Source/LarGridPivot.Grid.pas
          git commit -m "Clean config area color patch [config-cleanup-applied]"
          git push origin HEAD:feature-config-area-color-final
