name: Apply context copy
on:
  push:
    branches: [fix-row-selection-no-flicker]
permissions:
  contents: write
jobs:
  patch:
    if: "!contains(github.event.head_commit.message, '[context-copy-applied]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-row-selection-no-flicker
      - run: python apply_context_copy.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Source/LarGridPivot.Grid.pas
          git commit -m "Add Copy to pivot context menu [context-copy-applied]"
          git push origin HEAD:fix-row-selection-no-flicker
